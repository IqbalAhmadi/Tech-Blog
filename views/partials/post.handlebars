<a href="/views/{{id}}">
<div class="card">
    <h3 class="post-header">{{title}}</h3>
    <h4>Posted by {{user.username}} on 02/30/2023</h4>
    <div class="container">
        <p class="post-text">{{body}}</p>
    </div>
</div>
</a>